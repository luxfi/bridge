# Copy this to .env.test and fill in your values

# Test wallet credentials (use a dedicated test wallet!)
TEST_WALLET_PRIVATE_KEY=
TEST_WALLET_ADDRESS=

# Bridge endpoints
BRIDGE_UI_URL=http://localhost:3000
BRIDGE_API_URL=http://localhost:5000

# MPC nodes
MPC_NODE_URLS=http://localhost:6000,http://localhost:6001,http://localhost:6002

# Test networks
TEST_NETWORKS=ethereum,bsc,polygon,lux

# Test tokens
TEST_TOKENS=USDC,USDT,ETH,BNB

# Timeout settings (ms)
DEFAULT_TIMEOUT=10000
TRANSACTION_TIMEOUT=300000