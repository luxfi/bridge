{
  "name": "@luxbridge/e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright smoke + multi-tenant specs for bridge.lux.network and downstream tenants (Zoo, etc).",
  "license": "BSD-3-Clause",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:smoke": "playwright test tests/smoke.spec.ts",
    "test:multi-tenant": "playwright test tests/multi-tenant.spec.ts",
    "test:headed": "playwright test --headed",
    "install:browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "catalog:",
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}
