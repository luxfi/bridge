# goreleaser config for the bridge Go binaries.
#
# Triggered by the release workflow on `v*` tag push (same tag that
# drives the npm publish of @luxfi/bridge — the Go and TS sides ship
# together). Builds native binaries for all six policy platforms.
#
# Scope: cmd/bridge (the API + frontend host) — the canonical Go
# binary for this module. opnet-watcher/ is a separate Go module
# (its own go.mod) and ships from its own release pipeline; goreleaser
# cannot build it from the parent module's context.
#
# The Vite SPA + container images are built by .github/workflows/docker.yml
# (native multi-arch via hanzoai/.github reusable workflow).

version: 2

project_name: bridge

before:
  hooks:
    - go mod tidy

builds:
  - id: bridge
    main: ./cmd/bridge
    binary: bridge
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
    ldflags:
      - -s -w -X main.version={{.Version}} -X main.commit={{.Commit}} -X main.date={{.Date}}
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]

archives:
  - id: bridge
    ids: [bridge]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
    files:
      - LICENSE*
      - README*

checksum:
  name_template: checksums.txt

sboms:
  - artifacts: archive

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  use: github
  sort: asc

release:
  draft: false
  prerelease: auto
  mode: append
