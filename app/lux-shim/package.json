{
  "name": "@luxfi/bridge-shim",
  "version": "0.1.0",
  "private": true,
  "description": "Thin brand shim for bridge.lux.network — applies Lux brand to the upstream bridge3 image",
  "scripts": {
    "validate": "node scripts/validate-brand.mjs",
    "build:image": "docker build -t ghcr.io/luxfi/bridge:latest -f Dockerfile ."
  },
  "dependencies": {
    "@luxfi/core": "^10.0.6"
  }
}
