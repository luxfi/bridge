# api-bridge.lux.network answers 530 (no origin behind it); the deployed app has
# always called bridge-api.lux.network. Copying the old value into a .env built
# an app that could not load its settings at all.
VITE_SERVER_URI=https://bridge-api.lux.network
VITE_NET_VERSION=testnet
