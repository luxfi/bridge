apiVersion: v1
kind: Namespace
metadata:
  name: lux-bridge
---
# NATS Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nats
  namespace: lux-bridge
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nats
  template:
    metadata:
      labels:
        app: nats
    spec:
      containers:
      - name: nats
        image: nats:2.10-alpine
        args: ["-js", "-m", "8222", "--store_dir", "/data"]
        ports:
        - containerPort: 4222
          name: client
        - containerPort: 8222
          name: monitor
        volumeMounts:
        - name: nats-storage
          mountPath: /data
        livenessProbe:
          tcpSocket:
            port: 4222
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          tcpSocket:
            port: 4222
          initialDelaySeconds: 5
          periodSeconds: 5
      volumes:
      - name: nats-storage
        persistentVolumeClaim:
          claimName: nats-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: nats
  namespace: lux-bridge
spec:
  selector:
    app: nats
  ports:
  - port: 4222
    targetPort: 4222
    name: client
  - port: 8222
    targetPort: 8222
    name: monitor
---
# Consul StatefulSet
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: consul
  namespace: lux-bridge
spec:
  serviceName: consul
  replicas: 1
  selector:
    matchLabels:
      app: consul
  template:
    metadata:
      labels:
        app: consul
    spec:
      containers:
      - name: consul
        image: hashicorp/consul:1.17
        args:
          - "agent"
          - "-server"
          - "-bootstrap-expect=1"
          - "-ui"
          - "-client=0.0.0.0"
          - "-data-dir=/consul/data"
        ports:
        - containerPort: 8500
          name: http
        - containerPort: 8600
          name: dns
          protocol: UDP
        volumeMounts:
        - name: consul-data
          mountPath: /consul/data
        env:
        - name: CONSUL_BIND_INTERFACE
          value: eth0
        livenessProbe:
          exec:
            command: ["consul", "members"]
          initialDelaySeconds: 30
          periodSeconds: 10
  volumeClaimTemplates:
  - metadata:
      name: consul-data
    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: 10Gi
---
apiVersion: v1
kind: Service
metadata:
  name: consul
  namespace: lux-bridge
spec:
  selector:
    app: consul
  ports:
  - port: 8500
    targetPort: 8500
    name: http
  - port: 8600
    targetPort: 8600
    protocol: UDP
    name: dns
---
# MPC Config
apiVersion: v1
kind: ConfigMap
metadata:
  name: mpc-config
  namespace: lux-bridge
data:
  config.yaml: |
    nats:
      url: nats://nats:4222
    consul:
      address: consul:8500
    environment: mainnet
    mpc_threshold: 2
    badger_password: "lux-bridge-mpc-prod-2026"
    event_initiator_pubkey: "c3f3511cd2f849fd61e81ea62a842fd33367ee8fb5b867bb900877c7fb72ce3b"
    max_concurrent_keygen: 2
    db_path: /data/mpc
    backup_enabled: true
    backup_period_seconds: 300
    backup_dir: /data/mpc/backups
  peers.json: |
    {
      "node0": "0ce02715-0ead-48ef-9772-2583316cc860",
      "node1": "c95c340e-5a18-472d-b9b0-5ac68218213a",
      "node2": "ac37e85f-caca-4bee-8a3a-49a0fe35abff"
    }
  node0_identity.json: |
    {"node_name":"node0","node_id":"0ce02715-0ead-48ef-9772-2583316cc860","public_key":"96e47112bab4cb83e59a0a222964fad096f02a9dc52e449097f8991470a141ab"}
  node1_identity.json: |
    {"node_name":"node1","node_id":"c95c340e-5a18-472d-b9b0-5ac68218213a","public_key":"d49b5caace90b8317accd728b7b4419baef2ff32a164411d9ac7c1f703e13f7e"}
  node2_identity.json: |
    {"node_name":"node2","node_id":"ac37e85f-caca-4bee-8a3a-49a0fe35abff","public_key":"e0a570f5e7b52f76ba54aca0578dcd0c7f6569b7640027664af5be2ba8f0cf49"}
---
apiVersion: v1
kind: Secret
metadata:
  name: mpc-identity-keys
  namespace: lux-bridge
type: Opaque
stringData:
  node0_private.key: "3f38e18df0cc601d460978087e888d1369bba70f07035289e7e1323bc1070eac96e47112bab4cb83e59a0a222964fad096f02a9dc52e449097f8991470a141ab"
  node1_private.key: "a014abefd56782aea80de6a50273d4fbe78d9953b929d5c72b19480a7e3a06a7d49b5caace90b8317accd728b7b4419baef2ff32a164411d9ac7c1f703e13f7e"
  node2_private.key: "0fae6fe2025d9f64b5d011e08dea9b7d7689a0ccadbf0942f07a475310d06895e0a570f5e7b52f76ba54aca0578dcd0c7f6569b7640027664af5be2ba8f0cf49"
  initiator.key: "7b341f0406e2098f96e8918866324fc36436a279cd7ea7e2f8cc016e7a194a65"
---
# MPC Node StatefulSet
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mpc-node
  namespace: lux-bridge
spec:
  serviceName: mpc-node
  replicas: 3
  selector:
    matchLabels:
      app: mpc-node
  template:
    metadata:
      labels:
        app: mpc-node
    spec:
      initContainers:
      - name: setup-identity
        image: alpine:latest
        command: ["/bin/sh", "-c"]
        args:
        - |
          IDX=$(hostname | sed 's/mpc-node-//')
          NODE_NAME="node${IDX}"
          echo "Setting up identity for ${NODE_NAME}"
          mkdir -p /app/identity
          cp /config/config.yaml /app/config.yaml
          cp /config/peers.json /app/peers.json
          # Copy ALL node identities (needed for verifying peer messages)
          for i in 0 1 2; do
            cp /config/node${i}_identity.json /app/identity/node${i}_identity.json
          done
          # Copy own private key
          cp /keys/${NODE_NAME}_private.key /app/identity/${NODE_NAME}_private.key
          chmod 600 /app/identity/${NODE_NAME}_private.key
          echo "Identity setup complete for ${NODE_NAME}"
        volumeMounts:
        - name: app-dir
          mountPath: /app
        - name: mpc-config
          mountPath: /config
        - name: mpc-keys
          mountPath: /keys
      containers:
      - name: mpc
        image: ghcr.io/luxfi/lux-mpc:latest
        command: ["/bin/sh", "-c"]
        args:
        - |
          IDX=$(hostname | sed 's/mpc-node-//')
          NODE_NAME="node${IDX}"
          echo "Starting MPC node: ${NODE_NAME}"
          cd /app && exec mpcd start -n "${NODE_NAME}"
        ports:
        - containerPort: 8080
          name: http
        - containerPort: 9090
          name: grpc
        env:
        - name: LOG_LEVEL
          value: debug
        volumeMounts:
        - name: app-dir
          mountPath: /app
        - name: mpc-data
          mountPath: /data
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
      volumes:
      - name: app-dir
        emptyDir: {}
      - name: mpc-config
        configMap:
          name: mpc-config
      - name: mpc-keys
        secret:
          secretName: mpc-identity-keys
  volumeClaimTemplates:
  - metadata:
      name: mpc-data
    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: 20Gi
---
apiVersion: v1
kind: Service
metadata:
  name: mpc-node
  namespace: lux-bridge
spec:
  selector:
    app: mpc-node
  clusterIP: None
  ports:
  - port: 8080
    targetPort: 8080
    name: http
  - port: 9090
    targetPort: 9090
    name: grpc
---
# Bridge Server Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: bridge-server
  namespace: lux-bridge
spec:
  replicas: 2
  selector:
    matchLabels:
      app: bridge-server
  template:
    metadata:
      labels:
        app: bridge-server
    spec:
      containers:
      - name: bridge
        image: ghcr.io/luxfi/bridge-server:latest
        ports:
        - containerPort: 3000
          name: http
        env:
        - name: NODE_ENV
          value: production
        - name: PORT
          value: "3000"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: bridge-secrets
              key: database-url
        - name: NATS_URL
          value: nats://nats:4222
        - name: CONSUL_URL
          value: http://consul:8500
        - name: NODE_ID
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: MPC_THRESHOLD
          value: "2"
        - name: MPC_TOTAL_NODES
          value: "3"
        - name: MPC_KEY_PREFIX
          value: bridge/mpc
        livenessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 5
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
---
apiVersion: v1
kind: Service
metadata:
  name: bridge-server
  namespace: lux-bridge
spec:
  selector:
    app: bridge-server
  ports:
  - port: 3000
    targetPort: 3000
    name: http
  type: LoadBalancer
---
# Bridge UI Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: bridge-ui
  namespace: lux-bridge
spec:
  replicas: 2
  selector:
    matchLabels:
      app: bridge-ui
  template:
    metadata:
      labels:
        app: bridge-ui
    spec:
      containers:
      - name: ui
        image: ghcr.io/luxfi/bridge-ui:latest
        ports:
        - containerPort: 3000
          name: http
        env:
        - name: NEXT_PUBLIC_BRIDGE_API
          value: https://bridge-api.lux.network
        - name: NEXT_PUBLIC_BACKEND_API
          value: https://bridge-api.lux.network
        - name: NEXT_PUBLIC_IDENTITY_API
          value: https://bridge-api.lux.network
        - name: NEXT_PUBLIC_EXPLORER_API
          value: https://bridge-api.lux.network
        - name: NEXT_PUBLIC_NETWORK_NAME
          value: mainnet
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: bridge-ui
  namespace: lux-bridge
spec:
  selector:
    app: bridge-ui
  ports:
  - port: 80
    targetPort: 3000
    name: http
  type: LoadBalancer
---
# PostgreSQL StatefulSet
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres
  namespace: lux-bridge
spec:
  serviceName: postgres
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
      - name: postgres
        image: postgres:15-alpine
        ports:
        - containerPort: 5432
          name: postgres
        env:
        - name: POSTGRES_USER
          value: bridge
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: bridge-secrets
              key: postgres-password
        - name: POSTGRES_DB
          value: bridge
        volumeMounts:
        - name: postgres-data
          mountPath: /var/lib/postgresql/data
        livenessProbe:
          exec:
            command: ["pg_isready", "-U", "bridge"]
          initialDelaySeconds: 30
          periodSeconds: 10
  volumeClaimTemplates:
  - metadata:
      name: postgres-data
    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: 50Gi
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: lux-bridge
spec:
  selector:
    app: postgres
  ports:
  - port: 5432
    targetPort: 5432
---
# Secrets
apiVersion: v1
kind: Secret
metadata:
  name: bridge-secrets
  namespace: lux-bridge
type: Opaque
stringData:
  database-url: "postgresql://bridge:bridge@postgres:5432/bridge"
  postgres-password: "bridge"
---
# PersistentVolumeClaim for NATS
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nats-pvc
  namespace: lux-bridge
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
---
# Ingress
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bridge-ingress
  namespace: lux-bridge
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  tls:
  - hosts:
    - bridge.lux.network
    - bridge-api.lux.network
    secretName: bridge-tls
  rules:
  - host: bridge.lux.network
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: bridge-ui
            port:
              number: 80
  - host: bridge-api.lux.network
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: bridge-server
            port:
              number: 3000